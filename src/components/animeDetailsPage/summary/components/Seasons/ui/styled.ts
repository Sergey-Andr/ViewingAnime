import { Box, styled } from "@mui/material";

export const SeasonWrapper = styled(Box)(() => ({
    padding: "15px",
    display: "flex",
}));

export const AboutSeasonContainer = styled(Box)(() => ({
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-around",
}));
