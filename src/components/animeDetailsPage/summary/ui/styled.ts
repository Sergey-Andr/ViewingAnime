import { Box, styled } from "@mui/material";

export const WrapperSummary = styled(Box)(() => ({
    display: "flex",
    flexDirection: "column",
    width: "100%",
    maxWidth: "825px",
    marginRight: "15px",
}));
