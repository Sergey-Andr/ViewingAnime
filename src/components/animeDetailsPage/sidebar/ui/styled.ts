import { Box, styled } from "@mui/material";

export const SidebarWrapper = styled(Box)(() => ({
    position: "relative",
    width: "360px",
}));

export const SidebarContainerImg = styled(Box)(() => ({
    marginBottom: "15px",
    width: "360px",
    height: "540px",
    background: "#3D3737",
    borderRadius: "10px",
}));
