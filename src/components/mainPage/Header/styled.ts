import { Box, styled } from "@mui/material";

export const Wrapper = styled(Box)(() => ({
    display: "flex",
    alignItems: "center",
    padding: "0 30px",
    boxShadow: "0 0 60px rgb(0 0 0)",
    background: "#000000B3",
}));
